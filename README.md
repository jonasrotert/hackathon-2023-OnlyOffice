# Vater Smart Hackathon 2023

## Getting started
`docker-compose up` bzw `docker-compose up -d` startet den DocumentServer mit integrierter Example Application.  

Die Editoren sind unter `http://localhost/example` erreichbar.  
